apiVersion: v1
kind: Service
metadata:
  name: {{ include "express-app-template.fullname" . }}
  labels:
    {{- include "express-app-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "express-app-template.selectorLabels" . | nindent 4 }}
